---
- name: Installing cQube
  hosts: localhost
  gather_facts: true
  remote_user: root
  vars_files:
    - ../memory_config.yml
    - ../.version

  roles:
          - angular
          - angular_admin

